{
  "name": "Chess960 Eval",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y stockfish && pip install -r requirements.txt"
}
